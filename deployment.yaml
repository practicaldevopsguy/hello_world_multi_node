apiVersion: batch/v1
kind: Job
metadata:
  name: hello-world
spec:
  template:
    metadata:
      name: hello-world-pod
    spec:
      containers:
      - name: hello-world
        image: 192.168.1.104:5000/practicaldevopsguy/hello_world
        imagePullPolicy: Never
      restartPolicy: Never