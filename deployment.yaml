apiVersion: batch/v1
kind: Job
metadata:
  name: hello-world-multi-node
spec:
  template:
    metadata:
      name: hello-world-multi-node-pod
    spec:
      containers:
      - name: hello-world
        image: 127.0.0.1:5000/practicaldevopsguy/hello_world_multi_node:latest
        imagePullPolicy: IfNotPresent
      restartPolicy: Never